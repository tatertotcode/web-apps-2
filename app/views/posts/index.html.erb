<div class="row">
  <% for post in @posts %>
    <div class="col-3">
      <h4><%= post["author"] %></h4>
      <img class="img-fluid w-full" src="<%= post["image"] %>" height="300">
      <p><%= post["body"] %></p>
    </div>
  <% end %>
</div>